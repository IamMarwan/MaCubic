[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --strict-config
markers =
    unit: Fast isolated unit tests
    integration: Tests using the API or database
    security: Authentication and authorization tests
    performance: Performance and timing tests